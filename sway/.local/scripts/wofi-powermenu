#!/usr/bin/env bash

CHOSEN=$(printf "Lock\nSuspend\nPower Off\nReboot\nLog Out" | wofi -i --show dmenu --prompt "Power")

case "$CHOSEN" in
    "Lock") swaylock -n -c 282C34 ;;
    "Suspend") systemctl suspend ;;
    "Power Off") poweroff ;;
    "Reboot") reboot ;;
    "Log Out") logout
esac
